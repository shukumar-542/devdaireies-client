/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
        domains: ['i.ibb.co', 'localhost', 'lh3.googleusercontent.com'],
        remotePatterns: [

        ],
    },
};